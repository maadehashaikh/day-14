<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head> 
<body>
  <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
    <div class="flex w-full justify-end">
      <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-3 inline-block">Logout</a>
    </div>
      <h5 class="mb-2">Edit your post 🚀</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea class=" w-1/3 resize-none bg-transparent border-2 border-grey-800 rounded-md mt-2 pt-3 pl-2 block" placeholder="What's in your mind ?" name="content"><%= post.content %></textarea>
        <input type="submit" class="px-3 py-2 bg-yellow-500 block mt-3 rounded-md w-40 text-sm text-black" 
        value="Update post">
      </form>
      
  </div>
</body>
</html>