<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head> 
<body>
  <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
    <div class="flex w-full justify-end">
      <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-3 inline-block">Logout</a>
    </div>
      <h3 class="text-3xl mb-5"><span class="font-bold text-blue-400 text-4xl ">Hello ,</span> <%= user.name  %>🎉</h3>
      <h5 class="mb-2">You Can Create A New Post 🚀</h5>
      <form action="/post" method="post">
        <textarea class=" w-1/3 resize-none bg-transparent border-2 border-grey-800 rounded-md mt-2 pt-3 pl-2 block" placeholder="What's in your mind ?" name="content"></textarea>
        <input type="submit" class="px-3 py-2 bg-blue-500 block mt-3 rounded-md w-40 text-sm" 
        value="Create new post">
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts .</h3>
        <div class="postContainer mt-5">
          <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 p-4 border-2 border-zinc-600 rounded-md bg-zinc-700 mb-4">
              <h4 class="text-blue-500 mb-2 text-xl">@<%= user.username %></h4>
              <p class="tracking-tighter text-xl"><%= post.content %></p>
              <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
              <div class="btns flex mt-1 gap-4">
                <a class="text-blue-500" href="/like/<%= post._id %>">
                  <%= post.likes.indexOf(user._id) === -1  ? "Unlike" : "Like" %>
                </a>
                <a href="/edit/<%= post._id %>" class="text-zinc-500">Edit</a>
              </div>
            </div>
         <% }) %>
        </div>
      </div>
  </div>
</body>
</html>